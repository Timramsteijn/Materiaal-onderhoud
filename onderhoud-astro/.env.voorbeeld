# Kopieer dit bestand naar .env om het te gebruiken. .env staat in .gitignore.

# Sla tijdens ontwikkelen het inlogscherm over en werk als deze medewerker.
# Werkt alleen met `npm run dev`; in een productiebuild bestaat deze regel niet.
ONTWIKKEL_INLOG=t.verhoeven
